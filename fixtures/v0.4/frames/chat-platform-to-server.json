{
    "name": "frames-chat-platform-to-server",
    "note": "平台聊天（平台→服务器方向）：事件帧无 id，body 字段为 sender。channel 未绑定进服务端绑定表时业务层静默丢弃（debug 日志）——schema 层不区分绑定与否。",
    "source": "KuroAdapter@59d3be7 bridge/core/src/__tests__/server.test.ts「握手后平台 chat（带 channel）触发 onPlatformChat；握手前丢弃」+ bridge/core/src/__tests__/relay.test.ts 未绑定频道丢弃用例",
    "frames": [
        {
            "dir": "peer->server",
            "frame": {
                "header": { "type": "chat" },
                "body": { "channel": "114514", "sender": "群友A", "content": "你好 MC" }
            }
        }
    ],
    "expect": {
        "schema": "accept",
        "behavior": { "reply": null, "close": null }
    }
}
